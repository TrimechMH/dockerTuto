language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t mhtrimech/react-test -f ./../frontend/Dockerfile.dev .

script:
  - docker run -e CI=true mhtrimech/react-test npm run test